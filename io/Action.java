package io.github.bbodin.yncgamelab.io;
/***
 *
 * Action interface federates all kind of Actions.
 * Please note that it is only by name and that it
 * does not has anything special yet.
 *
 * Note: The action system could be queued,
 * but then We should use Action Factories to produce actions and queue then,
 * the grid would apply the actions.
 * then with a create function we can generate Actions
 */


public interface Action {
}
